// `cn` moved to @dispatch/ui (packages/ui) with the components that use it; this re-export
// keeps the app's long-standing `@/lib/utils` import path working unchanged.
export { cn } from '@/ui/lib/utils';
