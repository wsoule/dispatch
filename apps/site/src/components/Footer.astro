<footer>
  <div class="container">
    <code class="mono">brew install --cask wsoule/tap/dispatch</code>
    <p>
      Built with itself. This repo's backlog is in <code class="mono">.dispatch/</code>, and
      commits tagged <code class="mono">(run r-…)</code> were written by its agents.
    </p>
    <nav>
      <a href="https://github.com/wsoule/dispatch">GitHub</a>
      <a href="#install">Install</a>
    </nav>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--border-default);
    padding: var(--space-8) 0;
    color: var(--text-muted);
    font-size: var(--text-sm);
  }
  footer nav {
    display: flex;
    gap: var(--space-5);
    margin-top: var(--space-4);
  }
  footer a {
    color: var(--text-secondary);
    text-decoration: none;
  }
</style>
