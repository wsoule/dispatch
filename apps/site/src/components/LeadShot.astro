<section class="lead-shot">
  <div class="container">
    <figure>
      <img src="/shots/tasks.jpg" alt="The Dispatch board: tasks grouped by status, with live agent sessions" loading="eager" />
      <figcaption>The board. Every agent, every task, right now.</figcaption>
    </figure>
  </div>
</section>

<style>
  figure {
    margin: 0;
  }
  img {
    width: 100%;
    height: auto;
    display: block;
    border: 1px solid var(--border-default);
    border-radius: var(--radius-card);
    box-shadow: var(--shadow-card);
  }
  figcaption {
    margin-top: var(--space-3);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }
</style>
