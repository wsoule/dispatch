<section class="section">
  <div class="container">
    <p class="eyebrow">Source</p>
    <h2>Open core.</h2>
    <p>The engine is MIT. The app is source available. Read it, run it, extend it.</p>
    <p class="receipt">
      Built with itself. This repo's backlog is in <code class="mono">.dispatch/</code>, and
      commits tagged <code class="mono">(run r-…)</code> were written by its agents.
    </p>
    <p><a href="https://github.com/wsoule/dispatch">Read the source on GitHub →</a></p>
  </div>
</section>

<style>
  p {
    max-width: 36rem;
    color: var(--text-secondary);
  }
  .receipt {
    color: var(--text-muted);
    font-size: var(--text-sm);
  }
  a {
    text-decoration: none;
    color: var(--text-primary);
    font-weight: 500;
  }
</style>
