$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'typescript'
# Imported by cli/mcp/demo (library-layer packages), so graph-wise this is a
# library despite running standalone under bun.
layer: 'library'
